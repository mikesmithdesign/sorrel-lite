# Sorrel Lite — free one-page template for hair salons & barbershops

A single-file HTML template with editorial type over a clay-warm palette,
arch-shaped photography, a booking form and a live open/closed badge. No
build step, no frameworks — open `index.html` and edit.

**Live demo:** https://sorrel-lite.vercel.app

## Quick start

1. Edit the text directly in `index.html` — salon name, services, hours,
   contact details.
2. Replace the photos in `img/` with your own (keep the filenames, or
   update the `src` paths).
3. Set your opening hours in the `HOURS` table near the top of `site.js`
   (day 0 = Sunday; `[9, 18]` = 9am–6pm; `null` = closed).
4. Upload anywhere — it's static files. Vercel, Netlify and GitHub Pages
   host it free.

## License

Free for personal and commercial use. Attribution appreciated but not
required. Don't resell or redistribute the template itself as a template.

Demo photos are Unsplash placeholders for preview only — replace them
with your own photography before going live.

## Want more?

The full Sorrel theme adds a multipage price list with tap-to-book price
chips, a two-stylist team page, a tone-filterable work gallery with a
lightbox, four colour packs with a one-click switcher, and a JSON data
layer so the whole site rebrands from a handful of files — built as an
Astro 7 project.

→ https://mikesmithdesign.gumroad.com/l/sorrel-astro-theme (£20)
